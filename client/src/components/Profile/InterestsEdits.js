/*import React, { useState, useEffect } from 'react';
import { Typography, Button } from '@mui/material';
import Interests from './Interests'; // Assuming Interests component is in a separate file

const InterestsEdits = ({ hobbies, selectedHobbies, handleHobbyChange, handleSaveInterests }) => {
    return (
        <div>
            <Typography variant="h4" gutterBottom>
                Interests/Hobbies
            </Typography>
            <Interests
                hobbies={hobbies}
                selectedHobbies={selectedHobbies}
                handleHobbyChange={handleHobbyChange}
            />
            <Button onClick={handleSaveInterests} variant="contained" color="primary" style={{ marginTop: '1rem' }}>
                Save Interests
            </Button>
        </div>
    );
};

export default InterestsEdits;

*/